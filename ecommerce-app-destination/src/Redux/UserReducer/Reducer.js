const initialState={
    isLoading:false,
    message:"",
    isError:false,
    token:"",
    isAuth:false,
    userName:""
}

export const Reducer=((state=initialState,{type,payload})=>{
    switch (type) {
       
        default: return state
           
    }
})